{
  "_id": "{{@ system @}}-{{@ datatype @}}-collect",
  "namespaced_identifiers": false,
  "source": {
    "operation": "list",
    "properties": {
      "endpoint": "{{@ endpoint @}}"
    },
    "system": "{{@ system @}}",
    "type": "rest"
  },
  "type": "pipe"
}
{
  "_id": "{{@ system @}}",
  "headers": {
    "Content-Type": "application/json"
  },
  "oauth2": {
    "access_token": "$SECRET(oauth_access_token)",
    "client_id": "$SECRET(oauth_client_id)",
    "client_secret": "$SECRET(oauth_client_secret)",
    "refresh_token": "$SECRET(oauth_refresh_token)",
    "refresh_window": -10,
    "token_url": "{{@ token_url @}}"
  },
  "operations": {
    "addressRegions-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "next_page_link": "{{ body['$next'] }}",
      "next_page_termination_strategy": [
        "next-page-link-empty"
      ],
      "page_size": 100,
      "payload_property": "$items",
      "url": "/address_regions?attributes=all&items_per_page={{ page_size }}"
    },
    "addresses-delete": {
      "method": "DELETE",
      "url": "addresses/{{ entity.id }}"
    },
    "addresses-insert": {
      "method": "POST",
      "url": "/addresses"
    },
    "addresses-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "next_page_link": "{{ body['$next'] }}",
      "next_page_termination_strategy": [
        "next-page-link-empty"
      ],
      "page_size": 100,
      "payload_property": "$items",
      "since_property_name": "updated_or_created_since",
      "updated_expression": "{{ updated_at }}",
      "url": "/addresses?attributes=all&items_per_page={{ page_size }}"
    },
    "addresses-lookup": {
      "method": "GET",
      "url": "/addresses/{{ entity.id }}"
    },
    "addresses-update": {
      "method": "PUT",
      "url": "/addresses/{{ entity.id }}"
    },
    "contactPersons-delete": {
      "method": "DELETE",
      "url": "contact_persons/{{ entity.id }}"
    },
    "contactPersons-insert": {
      "method": "POST",
      "url": "/contact_persons"
    },
    "contactPersons-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "next_page_link": "{{ body['$next'] }}",
      "next_page_termination_strategy": [
        "next-page-link-empty"
      ],
      "page_size": 100,
      "payload_property": "$items",
      "since_property_name": "updated_or_created_since",
      "updated_expression": "{{ updated_at }}",
      "url": "/contact_persons?attributes=all&items_per_page={{ page_size }}"
    },
    "contactPersons-lookup": {
      "method": "GET",
      "url": "/contact_persons/{{ entity.id }}"
    },
    "contactPersons-update": {
      "method": "PUT",
      "url": "/contact_persons/{{ entity.id }}"
    },
    "contacts-delete": {
      "method": "DELETE",
      "url": "contacts/{{ entity.id }}"
    },
    "contacts-insert": {
      "method": "POST",
      "url": "/contacts"
    },
    "contacts-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "next_page_link": "{{ body['$next'] }}",
      "next_page_termination_strategy": [
        "next-page-link-empty"
      ],
      "page_size": 100,
      "payload_property": "$items",
      "since_property_name": "updated_or_created_since",
      "updated_expression": "{{ updated_at }}",
      "url": "/contacts?attributes=all&items_per_page={{ page_size }}"
    },
    "contacts-lookup": {
      "method": "GET",
      "url": "/contacts/{{ entity.id }}"
    },
    "contacts-update": {
      "method": "PUT",
      "url": "/contacts/{{ entity.id }}"
    },
    "countries-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "next_page_link": "{{ body['$next'] }}",
      "next_page_termination_strategy": [
        "next-page-link-empty"
      ],
      "page_size": 100,
      "payload_property": "$items",
      "url": "/countries?attributes=all&items_per_page={{ page_size }}"
    },
    "currencies-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "next_page_link": "{{ body['$next'] }}",
      "next_page_termination_strategy": [
        "next-page-link-empty"
      ],
      "page_size": 100,
      "payload_property": "$items",
      "url": "/currencies?attributes=all&items_per_page={{ page_size }}"
    },
    "list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "payload_property": "$items",
      "url": "{{ properties.endpoint }}"
    },
    "lookup": {
      "method": "GET",
      "url": "{{ properties.endpoint }}/{{ entity.id }}"
    },
    "salesInvoices-delete": {
      "method": "DELETE",
      "url": "sales_invoices/{{ entity.id }}"
    },
    "salesInvoices-insert": {
      "method": "POST",
      "url": "/sales_invoices"
    },
    "salesInvoices-list": {
      "id_expression": "{{ id }}",
      "method": "GET",
      "next_page_link": "{{ body['$next'] }}",
      "next_page_termination_strategy": [
        "next-page-link-empty"
      ],
      "page_size": 100,
      "payload_property": "$items",
      "since_property_name": "updated_or_created_since",
      "updated_expression": "{{ updated_at }}",
      "url": "/sales_invoices?attributes=all&items_per_page={{ page_size }}"
    },
    "salesInvoices-lookup": {
      "method": "GET",
      "url": "/sales_invoices/{{ entity.id }}"
    },
    "salesInvoices-update": {
      "method": "PUT",
      "url": "/sales_invoices/{{ entity.id }}"
    }
  },
  "type": "system:rest",
  "url_pattern": "https://api.accounting.sage.com/v3.1%s",
  "verify_ssl": true
}